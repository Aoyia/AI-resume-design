# 简历项目顺序调整设计文档

调整简历配置文件中项目经历的显示顺序，将“得小帆智能 Agent 平台”移动到首位。

## 变更背景

用户希望在其简历中将与 AI/Agent 相关的项目（“得小帆智能 Agent 平台”）前置，以突出 AI 开发经验。

## 变更范围

修改以下文件：
- [前端(agent 应用)_4年低代码经验_杨忠源_15779736785.json](file:///Users/neoyuan/Desktop/aoyi/AI-resume-design/前端(agent%20应用)_4年低代码经验_杨忠源_15779736785.json)

## 详细设计

在 `projects` 数组中：
- 将 ID 为 `proj-3` 的项目对象（得小帆智能 Agent 平台）移至数组的第一项（索引 0）。
- 原本排在第一项和第二项的 `proj-1` 和 `proj-2` 依次往后移动，保持原相对顺序，即排在索引 1 和索引 2。

调整后的 `projects` 结构如下：

```json
  "projects": [
    {
      "id": "proj-3",
      "company": "得小帆智能 Agent 平台",
      "position": "核心开发",
      "startDate": "2025.12",
      "endDate": "2026.03",
      "description": "项目简介：该项目是 AI 智能体（Agent）的前端核心对话与运行工作台，采用多页面应用（MPA）架构，主要负责大模型流式交互开发、Agent 提示词调优，并通过 iframe 桥接机制与低代码（aPaaS）平台进行深度业务联动。\n\n技术栈：\nvite + TypeScript + Sass + Vue 3 + SSE + Docker + Jenkins\n\n核心贡献：\n- 设计 **AI 动态组件安全渲染机制**：白名单注册 + **Zod 运行时 Schema 校验**两层防护，支持 AI 生成的图表、表格等动态内容安全渲染，解决 AI 输出类型不确定性导致的 XSS 风险\n- **大模型流式对话交互**：基于 **HTTP SSE** 实现了平滑的打字机流式输出；精细化设计了多模态消息数据结构，支持大模型**思维链（CoT）**推理展示、**Tool Calling** 工具调用以及多模态附件的流式渲染。\n- 设计 **iframe + postMessage 双向通信桥接方案**，解决跨域 SSO token 透传与 aPaaS 表单权限校验问题，结合 AI 自动预填字段，用户操作路径缩短 60%，无需页面跳转完成表单填报。"
    },
    {
      "id": "proj-1",
      "company": "得帆云低代码平台",
      "position": "核心开发",
      "startDate": "2022.06",
      "endDate": "2026.05",
      "description": "项目简介：\n企业级低代码应用开发平台，支持通过可视化拖拽快速搭建业务应用。平台采用微内核架构，核心包括表单引擎、规则引擎、流程引擎、插件系统等模块，前端代码规模 **92 万行**，服务 **500+** 企业客户。\n\n技术栈：Vue 2 + Vuex + TypeScript + Webpack + Babel + Element-UI + Vxe-Table + AntV X6\n\n核心贡献：\n- **设计并实现 DSL 规则引擎**：自研 **Babel 插件**将运算符重载，配合 **BigNumber.js** 实现零精度误差；通过 **`new Function()`** + 变量屏蔽隔离 **`window`/`document`** 实现受限执行；编译结果缓存复用，支撑 50+ 内置函数与复杂联动场景。\n- **优化规则执行策略**：将「全量计算」改为「按**依赖图**触发」，建立字段依赖关系，结合 **300ms** 防抖批处理，经日志实测复杂场景性能提升 **80%**，解决客户反馈的 **3000+** 数据表单卡顿问题。\n- **主导运行时性能优化**：针对钉钉消息审批场景表单打开慢的问题，通过 **Performance 面板**定位瓶颈，将表单配置、表单数据、业务事件三个串行请求改为 `Promise.all` 并行加载；修改缓存策略，结合 **LRU 缓存**高频接口响应，表单打开时间从 **7s 降至 3.5s（↑50%）。**"
    },
    {
      "id": "proj-2",
      "company": "得帆云低代码平台插件专项",
      "position": "架构设计 & 核心开发",
      "startDate": "2024.06",
      "endDate": "2024.12",
      "description": "项目简介：\n针对不同租户需要差异化功能集合（钉钉集成、飞书集成、电子签名、数据加密等 20+ 业务模块）的定制需求，传统硬编码条件判断导致主包体积膨胀、功能无法热插拔。设计并实现前端插件化架构从根本上解决这一问题。\n\n技术栈：JavaScript + Vue 2 + Rollup + IndexedDB\n\n核心贡献：\n- **引擎核心**：`ExtensionEngine` 单例统一管理插件全生命周期；每次状态变更通过 **md5** 签名触发响应式更新，驱动视图层自动重渲染，实现功能热插拔。\n- **动态加载 + IndexedDB 缓存**：插件以独立 **JS/CSS** 产物形式部署，运行时动态创建 `<script>` 标签按需注入；引入 **IndexedDB** 作为本地缓存层，命中版本缓存时直接内联注入跳过网络请求，版本变更时自动更新缓存。\n- **打包方案**：插件用 **Rollup** 独立打包为 **UMD 产物**，Vue / 平台公共库标记为 **`external`**，加载时直接复用宿主环境全局变量，避免重复打包。\n- **HookManager 跨插件通信**：基于**发布-订阅**实现，`callHook` 广播并收集所有插件返回值；插件失效时自动注销订阅，避免内存泄漏。\n- **三层功能校验**：黑名单（运营全局禁用）→ 禁用状态（租户级关闭）→ 版本兼容，粒度细化到插件 → 模块 → 功能三级；后端通过响应头下发 **base64** 白名单，引擎自动计算差集生成黑名单。"
    }
  ]
```
